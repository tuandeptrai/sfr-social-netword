source 'https://rubygems.org'

gem 'rails', '4.1.1'
gem 'mysql2', '~> 0.3.16'
gem 'sass-rails', '~> 4.0.3'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.0.0'
gem 'therubyracer',  platforms: :ruby
gem 'turbolinks'
gem 'jquery-turbolinks'
gem 'jbuilder', '~> 2.0'
gem 'sdoc', '~> 0.4.0',          group: :doc
gem 'tzinfo-data', platforms: [:mingw, :mswin]
gem "thin"
# For views
gem 'haml-rails'
gem 'haml'
# For view helpers
gem "simple_form"
gem 'country-select', '>= 1.1.1'
# For develop enviroinment
group :development do
  gem 'faker'
  gem "better_errors"
  gem "binding_of_caller"
  # For hidden assets query
  gem 'quiet_assets'
  # Requests logging integrate with browser addon
  gem 'meta_request'
  # Better display query result in rails console
  gem 'hirb'
  # For models annotation
  gem 'annotate', ">= 2.6.0"
  gem 'debugger'
  gem 'active_record_query_trace'
end

# For authentication
gem 'devise'

# Pagination
gem 'kaminari'